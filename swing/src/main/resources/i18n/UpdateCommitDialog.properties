UpdateCommitDialog(noUncommitted) = No uncommitted changes
UpdateCommitDialog(Title) = Commit
UpdateCommitDialog(commitButton) = Commit
UpdateCommitDialog(cancelButton) = Cancel
UpdateCommitDialog(updateButton) = Update 
UpdateCommitDialog(commitMessage) = Committing...
UpdateCommitDialog(Message) = Modified words
UpdateCommitDialog(UpdateMessage) = Added or updated words
UpdateCommitDialog(authenticationMessage) = Login incorrect
UpdateCommitDialog(modifiedWords) = Modified words
UpdateCommitDialog(updatedWords) = Updated words
LoginDialog(Title) = Login
LoginDialog(Username) = username
LoginDialog(Password) = password
LoginDialog(SaveUserPassword) = save username and password
LoginDialog(loginIncorrect) = Wrong username or password
UpdateCommitDialog(commitFailedMessage) = An error occured while committing
UpdateCommitDialog(commitSuccesMessage) = Commit successfull
UpdateCommitDialog(updateSuccesMessage) = Update successfull
UpdateCommitDialog(connectionFailure) = Can't connect to server
BannerTitle(Message) = Commit
Banner(Message) = Banner message goes here, something about commiting I guess.
MessageDialog(Conflict) = Trying to update a word which is locally modified, please edit and select a translation
